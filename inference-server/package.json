{
  "name": "@studio/inference-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "./venv/bin/uvicorn src.dev_server:app --reload --host 0.0.0.0 --port 8000",
    "test": "./venv/bin/pytest tests/",
    "lint": "./venv/bin/ruff check src/",
    "format": "./venv/bin/black src/",
    "setup": "python3 -m venv venv && ./venv/bin/pip install -r requirements.txt"
  }
}
